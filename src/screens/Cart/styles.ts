import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		marginTop: 60,
	},
	containerTitle: { width: '100%' },
	title: {
		textAlign: 'left',
		fontWeight: '700',
		fontSize: 24,
		marginTop: 35,
		marginBottom: 35,
		marginHorizontal: 20,
	},
});

export default styles;
