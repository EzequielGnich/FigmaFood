import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: { marginTop: 35 },
	title: {
		paddingHorizontal: 20,
		fontSize: 14,
		fontWeight: '700',
	},
	flatListStyle: { padding: 20 },
});

export default styles;
